"use client";

import { Card, CardTitle } from "@/components/ui/card";

export default function Error(){

    return (
        <Card>
            <CardTitle>
                <p>Une Erreur a eté détécter</p>
            </CardTitle>
        </Card>
    )
}